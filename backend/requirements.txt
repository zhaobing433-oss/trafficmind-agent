# TrafficMind Agent 依赖清单
# 安装命令: pip install -r requirements.txt

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 环境变量
python-dotenv==1.0.1

# LangGraph 工作流
langgraph==0.2.60
langgraph-checkpoint==2.0.10

# OpenAI-compatible 客户端（用于调用 DeepSeek）
openai==1.58.1

# 测试
pytest==8.3.4
httpx==0.28.1

# RAG V2 (Phase 11)
chromadb>=0.5,<2.0
sentence-transformers>=3.0,<6.0
jieba>=0.42,<1.0
