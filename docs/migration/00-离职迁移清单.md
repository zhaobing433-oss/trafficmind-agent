# TrafficMind Agent 离职迁移清单

> 用途：离开当前电脑前，逐项勾选确保没有遗漏。
> 本清单基于 2026-08-19 迁移事实核查结果，只记录"怎么带走 + 带什么"，不含任何 Secret 值。

---

## 一、源码与 GitHub

| 项 | 值 |
|---|---|
| 仓库 | `zhaobing433-oss/trafficmind-agent` |
| remote | `https://github.com/zhaobing433-oss/trafficmind-agent.git` |
| 开发基线 | `master` |
| master / HEAD | `a3f5e98607b5ca091ace161217dcf94c95924b4c` |
| Phase18 | `MERGED_AND_VERIFIED`（PR #10） |
| Phase18 feature head | `8addd5af1d0cda0a649330e77e9fb14e5030ecd5`（历史） |
| Git LFS | 已安装但未使用（无 `.gitattributes`） |
| submodule | 无 |

**源码可从 GitHub 恢复：`YES`**

检查项：

- [ ] `master` 已包含 Phase18（`a3f5e98`）并 push 到 `origin/master`
- [ ] `docs/migration/` 4 份文档已独立 docs commit + push
- [ ] 4 个 local-only source 文件已备份
- [ ] `DEEPSEEK_API_KEY` 已安全保存
- [ ] `trafficmind.db` / Claude memory 已决定是否备份

> 说明：本地存在一个旧分支 `backup-before-master-push` 及 1 个旧 commit（Phase 8 代码），
> 内容已被 master 后续合并取代，**不是迁移阻塞项**，可忽略。

---

## 二、必须额外备份（GitHub 没有的本地源码）

以下 4 个文件没有 Git 等价版本，若不带走将丢失：

| path | 用途 | 为什么 GitHub 没有 | 新电脑恢复位置 |
|---|---|---|---|
| `backend/evaluation/_fix_labels.py` | 一次性标签修复脚本 | 未跟踪 | `backend/evaluation/_fix_labels.py` |
| `backend/tests/acceptance_real_eval.py` | 真实模型验收脚本 | 未跟踪 | `backend/tests/acceptance_real_eval.py` |
| `backend/tests/acceptance_workflow.py` | 工作流验收脚本 | 未跟踪 | `backend/tests/acceptance_workflow.py` |
| `backend/tests/test_phase12_restart_recovery.py` | Phase12 重启恢复测试 | 未跟踪 | `backend/tests/test_phase12_restart_recovery.py` |

- [ ] 4 个 local-only source 已单独备份到个人安全位置

> 注意：本轮**不 commit、不修改、不复制**这些文件，仅在文档中记录位置，由用户自己决定如何备份。

---

## 三、Secrets（绝不写真实值）

| 变量名 | 用途 | 必需 | Secret | 推荐值（仅非 secret） |
|---|---|---|---|---|
| `DEEPSEEK_API_KEY` | DeepSeek LLM 调用 | LLM 必需（核心可选） | ✅ 是 | `<个人安全位置单独保存>` |
| `DEEPSEEK_BASE_URL` | DeepSeek 接口地址 | 可选 | 否 | `https://api.deepseek.com` |
| `DEEPSEEK_MODEL` | DeepSeek 模型 | 可选 | 否 | `deepseek-chat` |
| `RAG_ALLOW_MODEL_DOWNLOAD` | 是否允许下载本地 embedding 模型 | 可选 | 否 | `true` |

操作：

- [ ] `DEEPSEEK_API_KEY` 已保存到个人密码管理器 / 加密位置（**不要放进普通迁移 zip**）
- [ ] 新电脑手动重新输入该 Key

> 绝对规则：迁移文档不记录、不读取、不复制真实 Secret 值；不创建包含真实 Key 的新文件。

---

## 四、高价值可选开发历史

按优先级排序（都不是运行必需，不带也能 clean start）：

| 项 | 大小 | 优先级 | 说明 |
|---|---|---|---|
| `backend/data/trafficmind.db` | ~175 MB | 高 | 保留 chat / collaboration / memory / workflow / simulation 开发历史 |
| `backend/data/trafficmind.db.backup-20260804-162829` | ~93.5 MB | 中 | 数据库快照 |
| Claude project memory（`~/.claude/projects/c--Users-25442-trafficmind-agent/memory/`） | 小 | 高 | 我的持久项目记忆（非代码，但帮助快速恢复上下文） |
| `artifacts/` | 0.81 MB | 低 | 历史验收/评估报告，可重新生成 |

- [ ] 已决定是否备份 `trafficmind.db`（若备份，必须在 backend 停止时复制）
- [ ] 已决定是否备份 Claude project memory

---

## 五、不应该复制（可重新生成 / 机器绑定 / 体积大）

| 路径 | 大小 | 原因 |
|---|---|---|
| `backend/.venv/` | ~1.07 GB | 重新 `pip install` 重建 |
| `frontend/node_modules/` | ~234 MB | 重新 `npm ci` 重建 |
| `frontend/dist/` | ~6 MB | 构建产物，可重建 |
| `backend/data/vector_db/` | ~52 MB | Chroma 向量，可由 source 重建 |
| `backend/data/rag_v2/` | ~6.7 MB | RAG 索引 SQLite，可由 source 重建 |
| `__pycache__/`、`.pytest_cache/`、`*.pyc` | 小 | 缓存 |
| `~/.cache/huggingface/` | ~4.1 GB | HuggingFace 模型，可重新下载 |
| 临时日志（如 `collab-nl-sse.log`） | 小 | 可丢弃 |

---

## 六、账号重新认证（不复制，新电脑重新登录）

- [ ] GitHub：新电脑 `git clone` 后通过 Git Credential Manager 浏览器登录（或新建 SSH key）
- [ ] Claude Code：新电脑重新 `claude` 登录/配置
- [ ] SSH：建议新电脑**重新生成**（不复制旧 `~/.ssh` 私钥）

> 现有 `gh` CLI 已安装但未登录；现有 SSH key 为个人 key，不建议作为迁移核心。

---

## 七、离开旧电脑前最终勾选

- [ ] 1. `git status --short` 确认无遗漏的 tracked source 修改
- [ ] 2. 确认 `master`（含 Phase18 merge `a3f5e98`）已 push
- [ ] 3. 4 个 local-only source 文件已备份
- [ ] 4. `DEEPSEEK_API_KEY` 已在安全位置保存（未写入普通 zip）
- [ ] 5. 已决定是否备份 `backend/data/trafficmind.db`
- [ ] 6. 已决定是否备份 Claude project memory
- [ ] 7. 确认 `docs/migration/` 4 份文档已独立 commit + push
- [ ] 8. 记住新电脑恢复入口：`docs/migration/01-本地环境与启动指南.md`
- [ ] 9. 记住恢复验收入口：`docs/migration/03-新电脑恢复验收清单.md`
- [ ] 10. 确认不依赖旧电脑任何 PATH / 环境变量 / 全局配置（全部可重建）

> 提交历史说明：`docs/migration/` 作为**独立 docs commit** 提交，未进入 Phase18 PR #10。

---

> 迁移入口文档：见同目录 `01-本地环境与启动指南.md`。
> 恢复验收标准：见同目录 `03-新电脑恢复验收清单.md`。
