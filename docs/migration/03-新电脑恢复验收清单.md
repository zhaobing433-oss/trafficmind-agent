# TrafficMind Agent 新电脑恢复验收清单

> 用途：拿到个人电脑后，严格按清单判断迁移是否成功。
> 以下期望值以 **2026-08-19 snapshot** 为基准；GitHub latest master 始终是最终 source of truth。

---

## A. Git

- [ ] `git clone` 成功
- [ ] remote 正确（`https://github.com/zhaobing433-oss/trafficmind-agent.git`）
- [ ] 默认分支为 `master`（`git branch` / `git status` 确认）
- [ ] master 正确（snapshot：`a3f5e98607b5ca091ace161217dcf94c95924b4c`，Phase18 PR #10 已 MERGED）
- [ ] 恢复基线：以 `git clone` 后的 latest master 为准，**不要** checkout `feature/stage-18-intelligent-planning`

> Phase18 feature head `8addd5a` 仅作为 Phase18 历史定位信息，不是新电脑默认运行基线。
> 若 master 后续继续发展，GitHub latest master 始终是最终 source of truth。

---

## B. Python

- [ ] Python 3.11 可用
- [ ] `python -m venv backend\.venv` 成功
- [ ] `pip install -r backend\requirements.txt` 成功
- [ ] import smoke 成功：
  ```powershell
  backend\.venv\Scripts\python.exe -c "import fastapi, uvicorn, openai, chromadb; print('ok')"
  ```

---

## C. Node

- [ ] Node 可用（`node --version`）
- [ ] `npm ci` 成功
- [ ] `npm run dev` 成功

---

## D. Backend

- [ ] `:8000` 有 listener
- [ ] `GET /health` 返回 200
- [ ] `GET /planning/plans` 返回 200
- [ ] 不存在第二个 TrafficMind backend（无 8001 旧进程）

---

## E. Frontend

- [ ] `:5173` 可访问
- [ ] `:5173/api/planning/plans` 与 `:8000/planning/plans` 结果一致
- [ ] Plan Center 正常显示计划
- [ ] 浏览器 console 无 fatal error

---

## F. DB

**clean start 模式（默认）：**

- [ ] 不复制旧 DB，backend 启动后 `trafficmind.db` 自动创建
- [ ] `/planning/plans`、`/health` 正常

**restore historical DB 模式（可选）：**

- [ ] backend 已停止
- [ ] 复制旧 `trafficmind.db` 到 `backend/data/`
- [ ] backend 启动
- [ ] 历史 row / chat / collaboration / memory 可读取

---

## G. LLM

- [ ] `DEEPSEEK_API_KEY` 已配置
- [ ] `/health` 返回 `llmAvailable=true`
- [ ] planner preview smoke 成功（不执行真实外部 action）

---

## H. RAG

- [ ] index 可初始化（`POST /rag/v2/index` 或 `/knowledge` 重建）
- [ ] model 下载 / hash 降级路径正常
- [ ] 不复制旧 Chroma 也能工作（`vector_db` 自动重建）

---

## I. Tests（最小恢复测试，不默认跑完整几百条）

从仓库根目录，依次：

```powershell
# Phase18 关键 smoke（Semantic Replanning）
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_phase18_semantic_replanning.py -q

# Phase18 round1 + round2
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_phase18_round1_acceptance.py backend\tests\test_phase18_round2_runtime.py -q

# Workflow 核心
backend\.venv\Scripts\python.exe -m pytest backend\tests\test_phase12_workflow_api.py backend\tests\test_phase12_workflow_executor.py -q
```

全量（可选，最后再跑）：

```powershell
backend\.venv\Scripts\python.exe -m pytest backend\tests -q
```

---

## J. Browser

- [ ] Plan Center 页面可打开
- [ ] Plan Detail 可打开
- [ ] 刷新（F5）正常
- [ ] Console 无 fatal error

---

## K. Claude

- [ ] Claude Code 重新登录
- [ ] 打开 repo 目录
- [ ] `CLAUDE.md` 可读
- [ ] 新 Session 读取 `docs/migration/02-当前项目状态快照.md` 恢复上下文

> 若备份了 Claude project memory（`~/.claude/projects/c--Users-25442-trafficmind-agent/memory/`），
> 作为**参考**恢复项目记忆即可，不要直接覆盖用户级 `~/.claude` 目录（token/登录需重新配置）。

---

## Final

仅当所有核心项通过：

```
TRAFFICMIND_FRESH_PC_RECOVERY: PASS
```
